-- =============================================================================
-- 007_ftp.sql — Seed FTP accounts
-- Depends on: 001_users.sql (client id=3), 003_sites.sql (site id=1)
-- FTP password hash injected by seed-panel.sh (same mechanism as users).
-- =============================================================================

-- seed-panel.sh injects the hash and runs the full INSERT.
-- This file documents the structure; seed-panel.sh renders the real SQL.
SELECT 1;
